python == 3.9.19 
pandas == 2.1.1
numpy == 1.26.2 
matplotlib
seaborn 
plotly
scikit-learn
imblearn 

-e.
# tells pip to install the current package in "editable" mode, changes to the code are reflected immediately without reinstalling the package. 
# This line also ensures that the setup.py file is executed, which is necessary for package setup.
